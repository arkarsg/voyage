// Name of database on App Services
const voyageDb = 'voyage_app';

// Collections within the DB
const usersCollection = 'User';

// Valid user stub
const validUser = {
  username: 'Bob',
  email: 'bob@example.com',
};

module.exports = {
  voyageDb,
  usersCollection,
  validUser,
};
