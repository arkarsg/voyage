{
  "name": "voyage-backend",
  "version": "1.0.0",
  "description": "Backend for Voyage application",
  "main": "index.js",
  "repository": "https://github.com/arkarsg/voyage",
  "author": "arkarsg",
  "license": "MIT",
  "scripts": {
    "format": "prettier --no-error-on-unmatched-pattern --write \"**/**/*.js\"",
    "lint": "eslint --no-error-on-unmatched-pattern \"**/**/*.js\" --fix",
    "test": "jest --passWithNoTests --runInBand --forceExit --detectOpenHandles --coverage",
    "precommit": "yarn format && yarn lint && yarn test"
  },
  "devDependencies": {
    "eslint": ">=5.16.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-json": "^4.0.0",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "mongodb": "^6.4.0",
    "prettier": "3.2.5"
  },
  "dependencies": {
    "bson": "^6.7.0",
    "dotenv": "^16.4.5",
    "realm": "^12.6.2"
  }
}
