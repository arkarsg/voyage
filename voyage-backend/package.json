{
  "name": "voyage-backend",
  "version": "1.0.0",
  "description": "Backend for Voyage application",
  "main": "index.js",
  "repository": "https://github.com/arkarsg/voyage",
  "author": "arkarsg",
  "license": "MIT",
  "scripts": {
    "format": "prettier --no-error-on-unmatched-pattern --write functions/**/*.{js, json}",
    "lint": "eslint --no-error-on-unmatched-pattern \"functions/**/*.{js, json}\" --fix",
    "test": "jest --passWithNoTests",
    "precommit": "yarn format && yarn lint && yarn test"
  },
  "devDependencies": {
    "eslint": ">=5.16.0",
    "eslint-config-google": "^0.14.0",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "prettier": "3.2.5"
  }
}
